-- =============================================================================
-- Seed data for local development
-- This file is executed by `supabase db reset` after migrations.
-- Do NOT use real/production data here — only safe development fixtures.
-- =============================================================================

-- -----------------------------------------------------------------------------
-- motivational_quotes — sample data for local dev
-- (table will be created via migrations; this seed assumes it exists)
-- -----------------------------------------------------------------------------

-- INSERT INTO public.motivational_quotes (id, text, author, created_at) VALUES
--   (gen_random_uuid(), 'The journey of a thousand miles begins with a single step.', 'Lao Tzu', now()),
--   (gen_random_uuid(), 'It does not matter how slowly you go as long as you do not stop.', 'Confucius', now()),
--   (gen_random_uuid(), 'Our greatest glory is not in never falling, but in rising every time we fall.', 'Confucius', now()),
--   (gen_random_uuid(), 'Believe you can and you''re halfway there.', 'Theodore Roosevelt', now()),
--   (gen_random_uuid(), 'The only way to do great work is to love what you do.', 'Steve Jobs', now());

-- NOTE: Uncomment the INSERT above once the motivational_quotes table migration is applied.
-- Add more seed data for other tables below as the schema evolves.
